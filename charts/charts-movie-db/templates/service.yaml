apiVersion: v1
kind: Service
metadata:
  name: movie-db
spec:
  type: {{ .Values.service.type }}
  selector:
    app: movie-db
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: postgres
